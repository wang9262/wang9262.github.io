# Public Assets (静态资源)

## 1. 地位 (Role)
存放网站的静态资源文件，如图片、字体、图标等。此目录下的文件会被 Astro 原样复制到构建输出目录的根路径下。

## 2. 逻辑 (Logic)
*   **直接访问**: 文件可以通过根路径 `/` 直接访问（例如 `/favicon.ico`）。
*   **媒体归档**: 文章图片按 `{Slug}/{Slug}-{Index}-{Name}` 规范归档在 `media/` 子目录下。

## 3. 约束 (Constraints)
*   **引用路径**: 在 Markdown 或组件中引用时，需使用绝对路径（如 `/media/...`）。
*   **不处理**: Astro 不会对这些文件进行打包或哈希处理。

## 4. 成员说明 (Members)
*   `media/`: 文章图片归档目录。
*   `fonts/`: 网站字体文件。
*   `images/`: 网站通用图片（头像、图标等）。
*   `favicon.png`: 网站图标。
